alias reload='source ~/.bash_profile'

alias vi='vim'
alias svi='sudo vim -u ~/.vimrc'

function gitdots(){
    
    cd ~
    
    if [ ! -d ~/.dotfiles ]; then
        git clone git@github.com:rnosworthy/.dotfiles.git    
    fi
    
    cd .dotfiles
    git pull
 
}

function sf(){
    find . -type f -exec grep -l $1 {} + ;
}

# Add to grep: color, line numbers
alias grep="grep --color -n "


